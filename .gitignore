# dependencies and build output
node_modules/
.next/
out/
coverage/
playwright-report/
test-results/

# local environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# runtime secrets / live config (payment keys + business identifiers)
src/data/api_settings.json
service-account*.json
google-service-account*.json
penaameen-website-*.json

# TypeScript incremental state
*.tsbuildinfo

# local editor/system files
.DS_Store

# supabase/CLI local temp artifacts (supabase/.temp has own gitignore)
.sb-*
sb-*.tmp.mjs
.baseline.sql
.diff-err.txt

/generated/prisma
.vercel
.env*

# Git worktrees (don't upload to deploys)
.worktrees/