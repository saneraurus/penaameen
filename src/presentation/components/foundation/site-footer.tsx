import { Container } from "@/presentation/components/foundation/container";

export function SiteFooter() {
  return (
    <footer>
      <Container>
        <p>
          Foundation mode: public content, catalog, policy, and provider data
          remain subject to the approved project gates.
        </p>
      </Container>
    </footer>
  );
}
